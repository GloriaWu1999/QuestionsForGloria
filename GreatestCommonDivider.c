#include <stdio.h>

int getGCD(int m, int n)
{
	//Delete this return line to start with your algorithm
	return 0;
}

int main()
{
	int m = 0, n = 0;
	scanf("%d %d", &m, &n);
	int ans = getGCD(m, n);
	printf("%d", ans);
	return 0;
}
